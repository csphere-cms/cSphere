<?xml version="1.0" encoding="utf-8" ?>
<language xmlns:xs="http://www.w3.org/2001/XMLSchema-instance"
    xs:noNamespaceSchemaLocation="http://www.csphere.eu/xml/schemas/language.xsd">
    <translation type="plugin" name="install" version_min="0.1" short="en" />
    <vendor>csphere</vendor>
    <version>0.1</version>
    <published>2012-03-01</published>
    <copyright>cSphere Team</copyright>
    <license>Simplified BSD License</license>
    <name>Install</name>
    <info>Provides a guided installation</info>
    <engine name="csphere" version_min="2013.0" />
    <icon type="famfamfam">gb</icon>
    <authors>
        <current>cSphere Team</current>
        <past>cSphere Team</past>
    </authors>
    <contact>
        <email adress="contact@csphere.eu" />
        <web url="http://www.csphere.eu" />
    </contact>
    <definitions>
        <define name="admin_ok">Admin created with success</define>
        <define name="ajax_full">Enhanced AJAX</define>
        <define name="ajax_info">Loads main content independent of other website parts</define>
        <define name="cache_info">None is for developers only. File should only be used if nothing else works.</define>
        <define name="cache_ok">Memory cache settings are fine</define>
        <define name="cache_type">Cache type</define>
        <define name="conf">Configuration</define>
        <define name="config_ok">Configuration created with success</define>
        <define name="database_ok">Database settings are fine</define>
        <define name="db">Database</define>
        <define name="db_file">Filename</define>
        <define name="db_file_info">Required at SQLite, optional at LocalDB, prepend no path</define>
        <define name="db_prefix">Prefix</define>
        <define name="db_prefix_info">Prepends database table names, letters only</define>
        <define name="db_schema">Schema</define>
        <define name="db_schema_info">Name of database on specified server</define>
        <define name="debug_mode">Debug mode</define>
        <define name="debug_info">Shows additional information for developers</define>
        <define name="error_logs">Log errors</define>
        <define name="host_info">Valid IP or DNS, e.g. localhost or 127.0.0.1</define>
        <define name="install">Install</define>
        <define name="lang">Language</define>
        <define name="language_ok">Next page is translated as requested</define>
        <define name="logs_info">Saves errors that occur for later evaluation</define>
        <define name="mail">Mail</define>
        <define name="mail_newline">Newlines</define>
        <define name="mail_newline_info">Use default unless the mail deamon can't handle it</define>
        <define name="mail_ok">Mail settings stored with success</define>
        <define name="mail_subject">Subject</define>
        <define name="mail_subject_info">This text prepends all mail subjects</define>
        <define name="mail_test_fail">Failed to send mail with given settings</define>
        <define name="mail_test_subject">Testmail</define>
        <define name="mail_test_text">If you can read this your mail settings are fine</define>
        <define name="memory">Memory</define>
        <define name="no_cache">Cache settings contain errors</define>
        <define name="no_cfg">Directory /csphere/config must be writable</define>
        <define name="no_db">Database configuration error</define>
        <define name="port">Port</define>
        <define name="port_info">The port that service is running on</define>
        <define name="pretty_urls">Pretty URLs</define>
        <define name="rewrite_info">Attention: Requires webserver specific URL rewrites, read the docs</define>
        <define name="server">Server type</define>
        <define name="test">Test</define>
        <define name="timeout">Timeout</define>
        <define name="timeout_info">Timeout in seconds</define>
        <define name="too_short">Username and Password must contain at least four characters</define>
        <define name="webmaster">Webmaster</define>
        <define name="welcome">Welcome</define>
        <define name="welcome_info">This installs the cSphere CMS. Click below to continue</define>
        <define name="zlib_compression">ZLIB compression</define>
        <define name="zlib_info">Compresses HTTP answers with gzip, recommended</define>
    </definitions>
</language>